#include "3-calc.h"

/**
 * op_add - adds two integers
 * @a: the first int 
 * @b: the second int
 *
 * Retuen: the sum 
 */
int op_add(int a, int b)

[
	return (a + b);
}

/**
 * op_sub - subtracts two integers
 * @a: the first int
 * @b: the second int
 *
 * Retuen: the difference
 */
int op_sub(int a, int b)
{
	return ( a - b);
}

/**
 * op_mul - multiplies two integers
 * @a: the first int
 * @b: the second int
 *
 * Retuen: the prodcut
 */
int op_mul(int a, int b)
{ 
	return ( a * b);
}
/**
 * op_mod - modulo  two integers
 * @a: the first int
 * @b: the second int
 *
 * Retuen: the remainder
 */
int op_mod(int a, int b)
{
	return (a % b);
}
/** 
 * op_div - multiplies two integers
 * @a: the first int
 * @b: the second int
 *
 * Retuen: the quotine
 */
int op_div(int a, int b)
{
        return (a / b);
}

